Q2. Good Pair

public class Solution {
    public int solve(ArrayList<Integer> A, int B) {
        for(int i=0;i<A.size();i++){
            for(int j=0;j<A.size();j++){
                if(i!=j && (A.get(i)+A.get(j) == B)) return 1;
            }
        }
        return 0;
    }
}
